specVersion: "v1beta"
serviceId: "blog-test"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blog-test-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
